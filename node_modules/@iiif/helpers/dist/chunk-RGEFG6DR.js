// src/fetch.ts
import { upgrade } from "@iiif/parser/upgrader";
function fetchAndUpgrade(input, init) {
  return fetch(input, init).then((resp) => resp.json()).then(upgrade);
}

export {
  fetchAndUpgrade
};
