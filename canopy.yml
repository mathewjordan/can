collection:
  uri: https://iiif.archive.org/iiif/3/abolition-now-images/collection.json
iiif:
  # Max number of manifests to process per chunk
  chunkSize: 20
  # Concurrent fetch/build workers per chunk
  concurrency: 3
