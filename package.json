{
  "name": "@canopy-iiif/app",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node -e \"require('@canopy-iiif/lib').build()\"",
    "dev": "node -e \"require('@canopy-iiif/lib').dev()\"",
    "test": "echo \"No tests yet\" && exit 0",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "release": "node scripts/guard-publish.js && changeset publish"
  },
  "dependencies": {
    "@canopy-iiif/lib": "workspace:*",
    "@samvera/clover-iiif": "^3.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
