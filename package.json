{
  "name": "@canopy-iiif/app",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w @canopy-iiif/ui run build && node -e \"require('@canopy-iiif/lib').build()\"",
    "dev": "npm -w @canopy-iiif/ui run watch & node -e \"require('@canopy-iiif/lib').dev()\"",
    "test": "echo \"No tests yet\" && exit 0",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "release": "node packages/helpers/guard-publish.js && changeset publish"
  },
  "dependencies": {
    "@canopy-iiif/ui": "workspace:*",
    "@canopy-iiif/lib": "^0.1.6",
    "@samvera/clover-iiif": "^3.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@mdx-js/mdx": "^3.1.0",
    "@mdx-js/react": "^3.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}