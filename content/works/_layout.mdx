export const HelloWorld = () => {
  return "hello world";
};

# {props.manifest?.label?.en?.[0] || String(props.manifest?.label || 'Untitled')}

<p>
  <strong>ID:</strong> {props.manifest?.id}
</p>

<HelloWorld />

## Canvases

<ul>
  {(props.manifest?.items || []).map((canvas) => (
    <li key={canvas.id}>{canvas.label?.en?.[0] || canvas.id}</li>
  ))}
</ul>
