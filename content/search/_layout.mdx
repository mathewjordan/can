# Search

<div>
  <div>
    {/* This is where the search form would go */}
    {props.search.form}
  </div>
  <label>{props.search.count} results found</label>
  <span>{props.search.summary}</span>
  <div>
    {/* This is where results would go */}
    {props.search.results}
  </div>
</div>
