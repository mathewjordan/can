# Sitemap

Here are the available pages:

<ul>
  {props.pages.map((p) => (
    <li key={p.href}>
      <a href={`/${p.href}`}>{p.title}</a>
    </li>
  ))}
  {props.pages.length === 0 && <li><em>No pages yet.</em></li>}
  
</ul>
